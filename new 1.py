def analyzeSession(sessionNumber, sessionCode):
	""" Analyzes the list of session code (in sessionCode) and writes to 
	"""

	
"""	
analyzeUser for loop
	add userCode[i] to currentCodeList
	
	is userCode[i] too long from userCode[i+1]?
		if true, call analyzeSession(currentCodeList, sessionNumber)
			--->	do all the shit in the (for entry in userCode) loop
					return sessionNumber += 1
			reset currentCodeList
			
			
"""

